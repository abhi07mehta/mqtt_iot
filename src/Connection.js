import React from "react";
import { Form } from "react-bootstrap";

const Connection = ()=>{
    return(
        <div>
            <Form>
                
            </Form>
        </div>
    );
}

export default Connection;